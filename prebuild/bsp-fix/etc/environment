LIBVA_DRIVER_NAME=msm
GST_VA_ALL_DRIVERS=1
# vah264dec ties with v4l2h264dec at PRIMARY+1 and loses, so Totem autoplugs the broken one.
GST_PLUGIN_FEATURE_RANK=vah264dec:MAX
# trixie's 1.26 plugins must be scanned before the BSP's 1.22.12 set in the same dir.
GST_PLUGIN_PATH=/usr/lib/aarch64-linux-gnu/gstreamer-1.0:/usr/lib/gstreamer-1.0
