From 4d10c0ea3e9335eefd705390a918e69083cc5de0 Mon Sep 17 00:00:00 2001
From: Your Name <you@example.com>
Date: Tue, 19 Aug 2025 21:00:24 +0800
Subject: [PATCH] f

---
 adb/services.c | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/adb/services.c b/adb/services.c
index d44b0c5068..70c115ceff 100644
--- a/adb/services.c
+++ b/adb/services.c
@@ -426,6 +426,8 @@ int service_to_fd(const char *name)
     } else if(!strncmp(name, "remount:", 8)) {
         ret = create_service_thread(remount_service, NULL);
     } else if(!strncmp(name, "reboot:", 7)) {
+	    if (strcmp(name, "reboot:") == 0)
+	    { system("/usr/sbin/reboot"); }
         void* arg = strdup(name + 7);
         if (arg == NULL) return -1;
         ret = create_service_thread(reboot_service, arg);
-- 
2.34.1

